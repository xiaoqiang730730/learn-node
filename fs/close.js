var fs = require('fs');

//*****************************
// 方法说明:
//     以异步的方式关闭文件。
// 语法：
//     fs.close(fd, [callback(err)])
//     由于该方法属于fs模块，使用前需要引入fs模块（var fs= require(“fs”) ）
// 接收参数：
//     fd                   文件open时传递的文件描述符。
//     callback         回调
//*********************************
//
//
//
